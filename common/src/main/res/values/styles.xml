<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ImagePickerTheme" parent="ef_BaseTheme">
        <item name="colorPrimary">@color/ray_red</item>
        <item name="colorPrimaryDark">@color/ray_gray</item>
        <item name="colorAccent">@color/ray_red</item>
    </style>

    <style name="Theme.Default" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/ray_red</item>
        <item name="colorPrimaryDark">@color/ray_gray</item>
        <item name="colorAccent">@color/ray_red</item>
        <item name="fontFamily">@font/roboto</item>
    </style>

    <style name="ContentMainBadgeTitle" parent="AppTheme">
        <item name="android:textColor">#a9a9a9</item>
        <item name="android:textSize">12sp</item>
    </style>
</resources>
